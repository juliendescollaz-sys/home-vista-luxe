/**
 * Page Paramètres pour le mode PANEL
 * Wrapper autour de la page Settings existante pour permettre une divergence future
 */
import Settings from "@/pages/Settings";

export function PanelSettings() {
  return <Settings />;
}
