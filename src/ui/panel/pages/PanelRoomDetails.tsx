/**
 * Page Détails de pièce pour le mode PANEL
 * Wrapper autour de la page RoomDetails existante pour permettre une divergence future
 */
import RoomDetails from "@/pages/RoomDetails";

export function PanelRoomDetails() {
  return <RoomDetails />;
}
