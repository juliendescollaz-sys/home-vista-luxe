/**
 * Page Scènes pour le mode PANEL
 * Wrapper autour de la page Scenes existante pour permettre une divergence future
 */
import Scenes from "@/pages/Scenes";

export function PanelScenes() {
  return <Scenes />;
}
