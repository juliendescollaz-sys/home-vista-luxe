/**
 * Page Détails Media Player pour le mode PANEL
 * Wrapper autour de la page MediaPlayerDetails existante pour permettre une divergence future
 */
import MediaPlayerDetails from "@/pages/MediaPlayerDetails";

export function PanelMediaPlayerDetails() {
  return <MediaPlayerDetails />;
}
