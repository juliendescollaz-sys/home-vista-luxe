/**
 * Page Sonos Zones pour le mode PANEL
 * Wrapper autour de la page SonosZones existante pour permettre une divergence future
 */
import SonosZones from "@/pages/SonosZones";

export function PanelSonosZones() {
  return <SonosZones />;
}
